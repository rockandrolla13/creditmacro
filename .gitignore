# Source PDFs (large; copyrighted / confidential — never commit)
*.pdf
# Extracted markdowns: now TRACKED in this PRIVATE repo (owner's decision). Contains copyrighted /
# STRICTLY CONFIDENTIAL Citi / Alaph material — keep this repository private.
# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ipynb_checkpoints/
.DS_Store
.playwright-mcp/
raw/
wiki/.fresh_reasoning_snapshot.json

# Private live-run capture transcripts (never committed)
runs/
papers/

# Thesis Tracker live DB — may contain private investment notes (never committed).
# Schema/migrations/code/tests/docs ARE tracked; the .sqlite data file is not.
data/*.sqlite
data/*.sqlite-shm
data/*.sqlite-wal

# Theme Hypothesis Ledger runtime append-only stores (regenerated; never committed).
# The code/docs/tests ARE tracked; the JSONL event & link logs are not.
data/ledger/

# Obsidian per-user UI state (keep shareable config under wiki/.obsidian/, ignore transient state)
wiki/.obsidian/workspace.json
wiki/.obsidian/workspace-mobile.json
wiki/.obsidian/cache
wiki/.obsidian/.DS_Store
demo/_wiki/
