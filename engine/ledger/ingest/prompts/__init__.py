# Pass B prompt templates (Phase 4). Tier-1 I3: the word 'polarity' MUST NOT
# appear in this package — polarity is computed downstream, never asked of an LLM.
# EvidenceLink MUST NOT be referenced in prompt builders. Kept empty until Phase 4.
