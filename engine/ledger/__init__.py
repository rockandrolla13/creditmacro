# Theme Hypothesis Ledger — Alaph Stage-1 substrate (event-sourced, bitemporal).
# Normative spec: docs/ledger/ONTOLOGY.md. Build status: docs/ledger/PLAN_TRACKER.md.
# Lives inside engine/ (not src/) and reuses schema, memory firewall, surveillance,
# llm_provider, firewall.freeze, discovery — see docs/ledger/ONTOLOGY_DELTA.md D-01.
